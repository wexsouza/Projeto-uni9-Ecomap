document.addEventListener('DOMContentLoaded', function () {
  var form = document.querySelector('#formDoador, #formColetor'); if (!form) return; var coletor = form.id === 'formColetor'; var telefone = document.getElementById('telefone'); var cpf = document.getElementById('cpf'); aplicarMascara(telefone, mascaraTelefone); aplicarMascara(cpf, mascaraCPF);
  form.addEventListener('submit', function (event) { event.preventDefault(); limparErros(form); esconderAlerta(); var nome = document.getElementById('nome').value.trim(); var email = document.getElementById('email').value.trim(); var endereco = document.getElementById('endereco').value.trim(); var senha = document.getElementById('senha').value; var senha2 = document.getElementById('senha2').value; var tel = telefone.value.trim(); var cpfValor = cpf ? cpf.value.trim() : ''; var estadoCivil = document.getElementById('estadoCivil'); var ok = true; if (nome.length < 3) { mostrarErro('nome', 'erroNome', 'Informe seu nome completo.'); ok = false; } if (!telefoneValido(tel)) { mostrarErro('telefone', 'erroTelefone', 'Informe um telefone valido.'); ok = false; } if (!emailValido(email)) { mostrarErro('email', 'erroEmail', 'Informe um e-mail valido.'); ok = false; } if (!endereco) { mostrarErro('endereco', 'erroEndereco', 'Informe seu endereco.'); ok = false; } if (coletor && !cpfValido(cpfValor)) { mostrarErro('cpf', 'erroCpf', 'Informe um CPF valido.'); ok = false; } if (coletor && !estadoCivil.value) { mostrarErro('estadoCivil', 'erroEstadoCivil', 'Selecione seu estado civil.'); ok = false; } if (senha.length < 6) { mostrarErro('senha', 'erroSenha', 'A senha precisa ter pelo menos 6 caracteres.'); ok = false; } if (senha !== senha2) { mostrarErro('senha2', 'erroSenha2', 'As senhas nao coincidem.'); ok = false; } if (!ok) return; var tipo = coletor ? 'coletor' : 'doador'; if (dbExiste(tipo, email, cpfValor)) { mostrarAlerta('Ja existe uma conta com este e-mail ou CPF.', 'erro'); return; } var usuario = { id: Date.now().toString(), nome: nome, email: email, telefone: tel, cpf: cpfValor, endereco: endereco, estadoCivil: estadoCivil ? estadoCivil.value : '', senha: senha }; dbAdicionarUsuario(tipo, usuario); sessaoSalvar({ tipo: tipo, id: usuario.id, nome: usuario.nome, logadoEm: new Date().toISOString() }); localStorage.setItem(DB_KEYS.lembrar, JSON.stringify({ tipo: tipo, identificador: email })); window.location.href = tipo === 'coletor' ? 'dashboard-coletor.html' : 'dashboard-doador.html'; });
});
